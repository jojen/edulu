for development (default) - Memory based H2 Database

$ mvn clean install
$ mvn tomcat:run

access http://localhost:8080/wikistudy

for production - MySQL [settings: /src/main/databases/mysql/META-INF/spring/database.properties]

$ mvn clean install -P prod

$ mvn tomcat:run
or copy webapp to servlet container




